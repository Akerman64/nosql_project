# Copier en .env.local et renseigner. .env.local est git-ignore.
# Mettre les valeurs entre guillemets (l'URI Atlas contient des & ).
LOCAL_URI="mongodb://localhost:27017"
DB_NAME="off_projet"
# Pour le rendu, pointer sur le cluster Atlas :
# ATLAS_URI="mongodb+srv://USER:PASSWORD@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0"
